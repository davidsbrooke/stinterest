namespace app {
  export class NavbarController {
    public status;

    public logout() {
    
    }

    constructor(

    ) {

    }
  }

  angular.module('app').controller("NavbarController", NavbarController);
}
